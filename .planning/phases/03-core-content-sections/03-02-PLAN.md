---
phase: 03-core-content-sections
plan: 02
type: execute
wave: 1
depends_on: ["02-02"]
files_modified: [index.html]
autonomous: false
must_haves:
  truths:
    - "User sees Pillars section with Standard technical axes (7 expandable accordions)"
    - "User sees Alliance mission blocks (5 descriptive blocks)"
    - "Pull quote 'Code is Law... but Architecture is Politics' displays with distinctive styling"
    - "Accordions expand/collapse on click with smooth animation"
  artifacts:
    - path: "index.html"
      provides: "Pillars section HTML with 2-column layout (accordions + mission blocks)"
      contains: '<section id="pillars"'
    - path: "index.html"
      provides: "7 accordion components for Standard technical axes"
      contains: '<button class="accordion-trigger"'
    - path: "index.html"
      provides: "JavaScript accordion controller with click handlers"
      contains: "function initAccordions"
    - path: "index.html"
      provides: "CSS for accordion animation and pull quote styling"
      contains: ".accordion-content"
  key_links:
    - from: "Accordion buttons"
      to: "initAccordions function"
      via: "click event listeners"
      pattern: "addEventListener\\('click'"
    - from: "Accordion content divs"
      to: "CSS max-height transition"
      via: "expanded class toggle"
      pattern: "\\.expanded.*max-height"
    - from: "Pull quote element"
      to: "CSS accent border styling"
      via: "blockquote.pull-quote class"
      pattern: "\\.pull-quote.*border-left"
---

<objective>
Create Pillars section with dual-pillar structure: Standard (7 expandable technical accordions) and Alliance (5 mission blocks), plus distinctive pull quote.

Purpose: Explain Open Buro's two-pillar approach—Standard provides technical orchestration, Alliance drives ecosystem adoption. Accordions allow deep technical detail without overwhelming the page. Pull quote reinforces the core philosophy: technical standards enable organizational coordination.

Output: Complete Pillars section with 7 functional accordions, 5 mission blocks, and styled pull quote. All content in English per project decisions.
</objective>

<execution_context>
@/Users/mmaudet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mmaudet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md

# Phase dependencies
# Phase 1: Foundation provides CSS design system
# Phase 2: Hero provides JavaScript patterns
# Plan 03-01: Creates Problem and Vision sections (independent, can run parallel)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pillars Section with 7 Accordions and 5 Alliance Blocks</name>
  <files>index.html</files>
  <action>
Add Pillars section after Vision section with 2-column layout.

**Section container:**
- ID: "pillars"
- Badge: "DEUX PILIERS" (uppercase)
- H2: "Standard + Alliance"
- Subtitle: "Technical orchestration meets ecosystem coordination"
- 2-column layout: Left = Standard accordions (55%), Right = Alliance blocks (45%)

**Left column: Standard Technical Axes (7 accordions)**

Each accordion has:
- Button trigger (h3 with chevron icon)
- Collapsible content panel
- Smooth expand/collapse animation

**Accordion 1: Application Integration**
- Title: "Application Integration"
- Content: "Unified SSO with OIDC flow, standard app packaging format, centralized registry for discovery, common settings API for consistent UX. Publishers integrate once, users authenticate once."

**Accordion 2: Cross-service Navigation**
- Title: "Cross-service Navigation"
- Content: "Shared home screen, unified navigation bars, global app grid with search, cross-app command palette, integrated support system. Users navigate the platform, not individual apps."

**Accordion 3: Data Intelligence**
- Title: "Data Intelligence"
- Content: "Business object definitions (contacts, documents, tasks), cross-app event streaming, semantic knowledge graph, unified RAG/search APIs. Data flows between apps with context preserved."

**Accordion 4: Platform Collaboration**
- Title: "Platform Collaboration"
- Content: "Cross-service workspaces, threaded comments on any object, notification center aggregating all apps, shared presence and status. Collaboration transcends app boundaries."

**Accordion 5: Inter-apps & AI**
- Title: "Inter-apps & AI"
- Content: "Capability/intent casting (apps expose functions to platform), unified video conferencing, shared file picker, integrated image editing. Apps compose into workflows, AI agents orchestrate across tools."

**Accordion 6: Security & Encryption**
- Title: "Security & Encryption"
- Content: "Platform-level end-to-end encryption vault, granular permission models, audit logging, secure secret storage. Security is orchestrated, not fragmented across apps."

**Accordion 7: Mobile & Desktop**
- Title: "Mobile & Desktop"
- Content: "Flagship mobile app (iOS/Android) with embedded webviews, dedicated native apps for core tools, desktop client (Electron/Tauri), augmented browser extension. One platform, all devices."

**Accordion HTML structure (per accordion):**
```html
<div class="accordion-item">
  <button class="accordion-trigger" aria-expanded="false" aria-controls="accordion-content-1">
    <h3>Application Integration</h3>
    <span class="accordion-icon">›</span>
  </button>
  <div id="accordion-content-1" class="accordion-content" role="region" aria-labelledby="accordion-trigger-1">
    <p>[content text]</p>
  </div>
</div>
```

**Right column: Alliance Mission (5 blocks)**

Non-interactive blocks (not accordions). Each block has icon, title, description.

**Block 1: A Manifesto**
- Icon: 📜
- Title: "A Manifesto"
- Description: "Open Buro exists to prevent European workplace fragmentation. Open source without orchestration creates chaos. We establish founding principles before code."

**Block 2: Open Governance**
- Icon: ⚖️
- Title: "Open Governance"
- Description: "Neutral foundation structure modeled on Linux Foundation, CNCF. No single vendor controls the standard. Transparent decision-making, public roadmap."

**Block 3: Adoption Actions**
- Icon: 🛠️
- Title: "Adoption Actions"
- Description: "Implementation support for publishers: reference architectures, migration guides, certification program. Alliance members don't just endorse—they implement."

**Block 4: International Visibility**
- Icon: 🌍
- Title: "International Visibility"
- Description: "European-scale promotion: conferences, government outreach, media relations. Make Open Buro the default assumption for workplace tech procurement."

**Block 5: Funding**
- Icon: 💶
- Title: "Funding"
- Description: "Resource mobilization through member contributions, public grants (EU innovation funds), commercial services. Sustainable funding model ensures long-term viability."

**CSS for accordions:**
```css
.accordion-item {
  border: 1px solid var(--color-border);
  border-radius: 8px;
  margin-bottom: 1rem;
  background: var(--color-bg-secondary);
  overflow: hidden;
}

.accordion-trigger {
  width: 100%;
  padding: 1.5rem;
  background: none;
  border: none;
  text-align: left;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  transition: background-color 0.2s;
}

.accordion-trigger:hover {
  background-color: var(--color-bg-hover);
}

.accordion-trigger h3 {
  margin: 0;
  font-size: 1.125rem;
  color: var(--color-text-primary);
}

.accordion-icon {
  font-size: 1.5rem;
  transition: transform 0.3s ease;
  color: var(--color-accent);
}

.accordion-item.expanded .accordion-icon {
  transform: rotate(90deg);
}

.accordion-content {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
}

.accordion-item.expanded .accordion-content {
  max-height: 500px; /* Large enough for longest content */
}

.accordion-content p {
  padding: 0 1.5rem 1.5rem 1.5rem;
  margin: 0;
  color: var(--color-text-secondary);
  line-height: 1.6;
}
```

**CSS for Alliance blocks:**
Same card styling as constat cards from Plan 03-01 (dark background, border, padding, hover effect).

**Responsive:**
- 2-column layout on desktop (≥768px)
- Single column on mobile: Standard accordions first, Alliance blocks below
- Alliance blocks stack vertically on both mobile and desktop
  </action>
  <verify>
1. Open index.html in browser
2. Scroll to Pillars section
3. Verify: "DEUX PILIERS" badge, "Standard + Alliance" heading
4. Verify: 2-column layout on desktop (accordions left, Alliance blocks right)
5. Verify: 7 accordion items display with chevron icons
6. Try clicking accordions (functionality added in Task 2)
7. Verify: 5 Alliance blocks display with icons and descriptions
8. Resize to mobile: verify single column layout (Standard above, Alliance below)
  </verify>
  <done>
Pillars section HTML exists with 2-column layout, 7 accordion items (structure only, no click behavior yet), 5 Alliance mission blocks with icons, responsive design works on mobile/desktop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Accordion JavaScript and Pull Quote</name>
  <files>index.html</files>
  <action>
Add accordion functionality and pull quote to complete Pillars section.

**Accordion JavaScript:**

Add `initAccordions()` function inside existing `<script>` tag:

```javascript
function initAccordions() {
  const accordionItems = document.querySelectorAll('.accordion-item');

  accordionItems.forEach((item, index) => {
    const trigger = item.querySelector('.accordion-trigger');
    const content = item.querySelector('.accordion-content');

    if (!trigger || !content) return;

    // Set unique IDs for ARIA
    trigger.setAttribute('id', `accordion-trigger-${index + 1}`);
    content.setAttribute('id', `accordion-content-${index + 1}`);
    trigger.setAttribute('aria-controls', `accordion-content-${index + 1}`);

    trigger.addEventListener('click', () => {
      const isExpanded = item.classList.contains('expanded');

      // Close all other accordions (optional: remove these two lines for multi-expand)
      accordionItems.forEach(otherItem => {
        if (otherItem !== item) {
          otherItem.classList.remove('expanded');
          otherItem.querySelector('.accordion-trigger').setAttribute('aria-expanded', 'false');
        }
      });

      // Toggle current accordion
      item.classList.toggle('expanded');
      trigger.setAttribute('aria-expanded', !isExpanded);

      // Smooth scroll to accordion if expanding (keeps it in view)
      if (!isExpanded) {
        setTimeout(() => {
          trigger.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }, 100);
      }
    });

    // Keyboard accessibility: Enter/Space triggers click
    trigger.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        trigger.click();
      }
    });
  });
}

// Call on DOMContentLoaded (add to existing initialization)
document.addEventListener('DOMContentLoaded', () => {
  initAccordions();
  // ... other initialization
});
```

**Behavior:**
- Single-expand: clicking one accordion closes others (remove close-others code for multi-expand)
- Smooth animation via CSS max-height transition
- ARIA attributes update (aria-expanded) for screen readers
- Keyboard accessible (Enter/Space keys trigger expand)
- Smooth scroll to expanded accordion keeps it in view

**Pull quote:**

Add pull quote at bottom of Pillars section (after both columns):

```html
<blockquote class="pull-quote">
  <p>"Code is Law... but Architecture is Politics"</p>
  <cite>— Open Buro founding principle</cite>
</blockquote>
```

**Pull quote CSS:**
```css
.pull-quote {
  margin: 4rem auto 0;
  padding: 2rem 0 2rem 2rem;
  border-left: 4px solid var(--color-accent);
  max-width: 800px;
  background: linear-gradient(90deg, rgba(var(--color-accent-rgb), 0.05) 0%, transparent 100%);
}

.pull-quote p {
  font-size: 1.75rem;
  font-weight: 600;
  font-family: var(--font-heading);
  color: var(--color-text-primary);
  margin: 0 0 0.5rem 0;
  line-height: 1.3;
  font-style: italic;
}

.pull-quote cite {
  font-size: 1rem;
  color: var(--color-text-secondary);
  font-style: normal;
  font-family: var(--font-body);
}
```

**Design notes:**
- Accent left border (4px, --color-accent)
- Gradient background (5% accent opacity fading to transparent)
- Large italic quote text (1.75rem)
- Smaller attribution (cite element)
- Centered, max-width 800px
- Spacing above (4rem margin-top) separates from columns

**Accessibility:**
- Semantic blockquote + cite elements
- Readable font size (1.75rem)
- High contrast (primary text color)
  </action>
  <verify>
1. Open index.html in browser
2. Scroll to Pillars section
3. Click first accordion ("Application Integration"):
   - **Expected:** Content panel slides open smoothly (300ms)
   - **Expected:** Chevron rotates 90 degrees
   - **Expected:** aria-expanded updates to "true"
4. Click second accordion ("Cross-service Navigation"):
   - **Expected:** First accordion closes, second opens
   - **Expected:** Only one accordion open at a time
5. Test keyboard navigation:
   - Tab to accordion trigger, press Enter or Space
   - **Expected:** Accordion expands/collapses
6. Scroll to pull quote at bottom of section:
   - **Expected:** Quote displays with accent left border
   - **Expected:** Gradient background visible
   - **Expected:** Large italic text: "Code is Law... but Architecture is Politics"
   - **Expected:** Attribution "— Open Buro founding principle" below
7. Check console: no JavaScript errors
  </verify>
  <done>
Accordions are fully functional (expand/collapse on click, smooth animation, single-expand behavior, keyboard accessible, ARIA attributes), pull quote displays with distinctive accent border and gradient background, no JavaScript errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Pillars section with:
- 7 expandable accordions for Standard technical axes (Application Integration, Cross-service Navigation, Data Intelligence, Platform Collaboration, Inter-apps & AI, Security & Encryption, Mobile & Desktop)
- 5 Alliance mission blocks (Manifesto, Open Governance, Adoption Actions, International Visibility, Funding)
- Fully functional accordion JavaScript with smooth expand/collapse animation
- Pull quote "Code is Law... but Architecture is Politics" with distinctive accent border styling
- 2-column responsive layout (accordions + mission blocks)
  </what-built>
  <how-to-verify>
**Visual verification:**

1. Open `index.html` in browser
2. Scroll to **Pillars section** ("DEUX PILIERS" badge):
   - **Expected:** 2-column layout on desktop (≥768px): accordions left, Alliance blocks right
   - **Expected:** "Standard + Alliance" heading with subtitle

**Accordion functionality:**

3. Click **"Application Integration"** accordion:
   - **Expected:** Content panel slides open smoothly (300ms animation)
   - **Expected:** Chevron icon (›) rotates 90 degrees to point down
   - **Expected:** Content reads: "Unified SSO with OIDC flow, standard app packaging format..."

4. Click **"Cross-service Navigation"** accordion:
   - **Expected:** First accordion closes automatically
   - **Expected:** Second accordion opens (single-expand behavior)

5. Test **keyboard navigation**:
   - Press Tab until accordion trigger is focused (visible focus ring)
   - Press **Enter** or **Space** key
   - **Expected:** Accordion expands/collapses

6. Expand each of the 7 accordions one by one:
   - **Expected:** All 7 have unique content (Application Integration, Cross-service Navigation, Data Intelligence, Platform Collaboration, Inter-apps & AI, Security & Encryption, Mobile & Desktop)
   - **Expected:** Content is readable, no truncation

**Alliance blocks:**

7. Verify **5 Alliance mission blocks** (right column):
   - **Expected:** Each has icon + title + description
   - **Expected:** Icons: 📜 Manifesto, ⚖️ Open Governance, 🛠️ Adoption Actions, 🌍 International Visibility, 💶 Funding
   - **Expected:** Hover over blocks → elevation effect (same as constat cards)

**Pull quote:**

8. Scroll to **pull quote** at bottom of Pillars section:
   - **Expected:** Large italic text: "Code is Law... but Architecture is Politics"
   - **Expected:** Accent left border (4px, colored to match design system accent)
   - **Expected:** Gradient background (subtle, fading left to right)
   - **Expected:** Attribution below: "— Open Buro founding principle"

**Responsive design:**

9. Resize browser to mobile width (<768px):
   - **Expected:** Single column layout: accordions above, Alliance blocks below
   - **Expected:** Pull quote remains centered, readable

**Issues to check:**
- Accordions not expanding on click
- Multiple accordions open at once (should be single-expand)
- Chevron icon not rotating
- Missing content in accordions
- Alliance blocks not displaying
- Pull quote border/gradient not visible
- Layout not stacking on mobile
- JavaScript errors in console
  </how-to-verify>
  <resume-signal>
Type **"approved"** if all checks pass.

If issues found, describe specifically:
- Which accordion or block has issues
- What behavior is broken (e.g., "Accordion 3 won't open", "Pull quote has no border")
- Browser/device used
  </resume-signal>
</task>

</tasks>

<verification>
**Overall phase checks:**

1. Requirements coverage:
   - PIL-01: Pillars section with badge, H2, 2-column layout ✓
   - PIL-02: Left column shows 7 expandable accordions ✓
   - PIL-03: Right column shows 5 Alliance mission blocks ✓
   - PIL-04: Pull quote displayed with distinctive styling ✓

2. Functionality:
   - Accordions expand/collapse on click
   - Single-expand behavior (one open at a time)
   - Smooth animation (300ms max-height transition)
   - Keyboard accessible (Enter/Space keys)
   - ARIA attributes update (aria-expanded)

3. Design system integration:
   - Uses CSS custom properties from Phase 1
   - Consistent card styling (Alliance blocks match constat cards)
   - Accordion styling integrates with design system tokens
   - Pull quote uses accent color from design system

4. Accessibility:
   - Semantic HTML (section, button, h3, blockquote, cite)
   - ARIA labels (aria-expanded, aria-controls, role="region")
   - Keyboard navigation (Tab, Enter, Space)
   - Focus visible (default browser outline or custom focus ring)

5. Responsive design:
   - 2-column layout on desktop (≥768px)
   - Single column layout on mobile (<768px)
   - Pull quote centered, max-width 800px
</verification>

<success_criteria>
1. Pillars section displays with 2-column layout (7 accordions + 5 Alliance blocks)
2. All 7 accordions expand/collapse on click with smooth animation
3. Single-expand behavior works (clicking one closes others)
4. Chevron icons rotate 90 degrees when expanded
5. All accordion content is readable and complete
6. 5 Alliance mission blocks display with icons and descriptions
7. Pull quote displays with accent left border and gradient background
8. Keyboard navigation works (Tab to focus, Enter/Space to toggle)
9. Responsive design works on mobile (single column, accordions above Alliance blocks)
10. No JavaScript errors in console
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content-sections/03-02-SUMMARY.md` following the summary template with:

**Artifacts created:**
- Pillars section HTML with 2-column layout
- 7 accordion components for Standard technical axes
- 5 Alliance mission blocks
- JavaScript function `initAccordions()` for expand/collapse behavior
- Pull quote blockquote with distinctive styling
- CSS for accordion animation, Alliance blocks, pull quote

**Key decisions:**
- Single-expand accordion behavior (close others when one opens)
- Smooth scroll to expanded accordion (keeps it in view)
- Alliance blocks use same card styling as Problem section for consistency
- Pull quote uses gradient background (5% accent opacity) for subtle emphasis
- max-height: 500px for accordion content (covers longest content without truncation)

**Integration points:**
- CSS custom properties from Phase 1 design system (--color-accent, --color-bg-secondary, etc.)
- JavaScript initialization pattern from Phase 2 (DOMContentLoaded event)
- Existing HTML structure (inserts after Vision section)

**Technical details:**
- Accordion animation: CSS max-height transition (300ms ease)
- ARIA attributes: aria-expanded, aria-controls, role="region"
- Keyboard events: Enter and Space keys trigger toggle
- Responsive breakpoint: 768px (2-column → single column)
</output>
